import { FormerrorDirective } from './formerror.directive';

describe('FormerrorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormerrorDirective();
    expect(directive).toBeTruthy();
  });
});
